---
format:
  name: build_it
  generator:
    name: build_it:json
---

checkNullSafety: true

classes:
- name: Category
  fields:
  - { name: id, type: int? }
  - { name: name, type: String? }
  - { name: products, type: List<Product>, jsonKey: { defaultValue: [] } }

- name: Product
  fields:
  - { name: id, type: int? }
  - { name: name, type: String? }